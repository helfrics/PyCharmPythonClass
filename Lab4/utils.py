import random


def simulate_gachapon(n):
    """
    Estimate the number of items you must buy to get all (n) of the prizes.
    :param n: Total number of prizes (an integer)
    :return: Number of iterations to obtain all prizes (an integer)
    """
    pool = []
    while 
